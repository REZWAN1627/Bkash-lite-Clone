<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rex.bkashliteclone">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BkashLiteClone">
        <activity android:name=".MainActivity" />
        <activity android:name=".MainUI.AppsMainUI" />
        <activity android:name=".UserUtils.Set_userProfile" />
        <activity
            android:name=".UserUtils.Set_userName"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Authenticate.Login_withPassword"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Authenticate.Mobile_OTP_verification"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Authenticate.Log_Registration"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".SplashScreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>